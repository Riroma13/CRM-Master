# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: 02-login-auth.spec.ts >> Login & Auth >> invalid credentials shows error
- Location: e2e/02-login-auth.spec.ts:23:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByText('Credenciales inválidas')
Expected: visible
Timeout: 10000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 10000ms
  - waiting for getByText('Credenciales inválidas')

```

```yaml
- text: C
- heading "CRM-Master" [level=1]
- paragraph: Portal del cliente
- paragraph: Request failed with status 401
- text: Email
- textbox "admin@ejemplo.com": wrong@email.com
- text: Contraseña
- textbox "••••••••": wrongpass
- button "Iniciar sesión":
  - img
  - text: Iniciar sesión
- alert
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | const ADMIN_EMAIL = 'admin@demo.local';
  4  | const ADMIN_PASS = 'password';
  5  | 
  6  | test.describe('Login & Auth', () => {
  7  |   test('login page renders correctly', async ({ page }) => {
  8  |     await page.goto('/login');
  9  |     await expect(page.getByText('CRM-Master')).toBeVisible();
  10 |     await expect(page.getByText('Portal del cliente')).toBeVisible();
  11 |     await expect(page.getByText('Iniciar sesión')).toBeVisible();
  12 |   });
  13 | 
  14 |   test('successful login redirects to admin', async ({ page }) => {
  15 |     await page.goto('/login');
  16 |     await page.fill('input[type="email"]', ADMIN_EMAIL);
  17 |     await page.fill('input[type="password"]', ADMIN_PASS);
  18 |     await page.click('button[type="submit"]');
  19 |     await page.waitForURL(/\/admin/, { timeout: 15000 });
  20 |     await expect(page.getByRole('heading', { name: 'Dashboard', exact: true })).toBeVisible();
  21 |   });
  22 | 
  23 |   test('invalid credentials shows error', async ({ page }) => {
  24 |     await page.goto('/login');
  25 |     await page.fill('input[type="email"]', 'wrong@email.com');
  26 |     await page.fill('input[type="password"]', 'wrongpass');
  27 |     await page.click('button[type="submit"]');
> 28 |     await expect(page.getByText('Credenciales inválidas')).toBeVisible({ timeout: 10000 });
     |                                                            ^ Error: expect(locator).toBeVisible() failed
  29 |   });
  30 | 
  31 |   test('admin pages redirect to login without session', async ({ page }) => {
  32 |     await page.goto('/admin');
  33 |     await expect(page).toHaveURL(/\/login/);
  34 |   });
  35 | });
  36 | 
```