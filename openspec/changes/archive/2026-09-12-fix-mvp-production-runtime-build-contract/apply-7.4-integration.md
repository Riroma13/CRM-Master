# Apply 7.4 Integration

**Status:** PASS

Production Compose renders with the production profile and safe empty env input. Stock Caddy validation passes and resolves `/api/*` to `api:3001` and remaining paths to `tenant-web:3000`.
